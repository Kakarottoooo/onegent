## Codebase Patterns

- Two-layer execution: AI (Stagehand/stagehand.act()) first, Playwright RPA fallback
- All browser automation flows through lib/booking-autopilot/stagehand-executor.ts
- Stage detection uses Claude Haiku perception (AI_LOOP_STAGE_DETECT) + rule-based signals
- Payment forms are always RPA-only (cross-origin iframe blocks AI access)
- React inputs require nativeSetter + input/change events — locator.fill() alone doesn't trigger React state
- Provider-specific code lives in lib/booking-autopilot/providers/
- booking-com.ts has ~2900 lines — read only the sections you need, not the whole file
- AI_LOOP_FULL=true enables all AI phases; individual flags: AI_LOOP_STAGE_DETECT, AI_LOOP_LISTING, AI_LOOP_FORM_FILL
- liveLogPush(jobId, line) for real-time log streaming; trace() calls this automatically

## 2026-04-09 - US-003
- What was implemented: Refactored stagehand-executor.ts to use provider registry (getProvider) instead of resolveProviderContext/resolveProviderIdForUrl; replaced cookie injection with provider.setup(); replaced isBookingCom boolean with provider object; replaced providerGetBookingComStageSignals calls with provider?.getStageSignals(); replaced fill calls with provider?.fillGuestForm?/fillPaymentForm?
- Files changed: lib/booking-autopilot/stagehand-executor.ts
- **Learnings for future iterations:**
  - provider is resolved 3 times: startProvider (at startUrl, before nav), bookingComContext (at currentUrl in attemptStageRecovery), activeProvider (at currentUrl for final signal/fill block)
  - bookingComSignals renamed to providerSignals; .finalPaymentState → .paymentStep; same .guestDetailsStep
  - provider?.getStageSignals() does NOT take 4th arg (visibleCheckoutFields) - only (page, url, text)
  - fs/path imports removed from executor since cookie injection moved to provider.setup()
  - providerGetBookingComStageSignals still imported for StageAssessmentDependencies (stage-assessment.ts uses it internally)
  - setup() is called BEFORE navigation — addCookies and addInitScript work; page.evaluate fails silently
---

## 2026-04-09 - US-002
- What was implemented: bookingComProvider object wrapping existing booking-com.ts functions, registered in global registry
- Files changed: lib/booking-autopilot/providers/booking-com.ts (added imports + bookingComProvider export + registerProvider call)
- **Learnings for future iterations:**
  - Added `import fs from "fs"`, `import path from "path"`, `import { safePressEscape }`, `import { registerProvider }`, `import type { BrowserProvider, ProviderStageSignals }` at top
  - getBookingComStageSignals() returns `{ isBookingCom, searchResults, hotelDetailPage, hotelDetailUrl, nonCheckoutUrl, guestDetailsStep, finalPaymentState }` — mapped to ProviderStageSignals: hotelDetail = hotelDetailPage || hotelDetailUrl, paymentStep = finalPaymentState
  - setup() takes (page, context, trace) — context is the V3Context wrapper passed as stagehand.context; use `(context as any).addCookies()` for cookies, `(context as any).browserContext ?? (context as any).context` for BrowserContext
  - All existing exports remain untouched per the additive-only requirement
---

## 2026-04-09 - US-001
- What was implemented: BrowserProvider interface, ProviderStageSignals interface, provider registry with getProvider/requireProvider, re-export index
- Files changed: lib/booking-autopilot/providers/types.ts (new), lib/booking-autopilot/providers/registry.ts (new), lib/booking-autopilot/providers/index.ts (new)
- **Learnings for future iterations:**
  - The existing resolveProviderContext/resolveProviderIdForUrl live in lib/booking-autopilot/core/provider-router.ts — leave untouched until US-003
  - profile/helpers typed as 'unknown' in BrowserProvider to avoid circular imports until US-003
  - context typed as 'unknown' in setup() to avoid importing BrowserContext from playwright-core
  - Registry uses simple array + find() — registerProvider() must be called to add providers
---
